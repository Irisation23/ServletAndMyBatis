<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.data.sal.SalGradesMapper">
  <select id="findAllSalGrades" resultType="Sal">
    SELECT grade, losal, hisal
    FROM SALGRADES;
  </select>
</mapper>